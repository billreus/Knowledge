集合：
1. Map:
   1. HashMap：hashmap底层是怎么实现的（hashmap的话是数组+链表实现的，通过hash散列化来决定进哪一个数组，如果有的话就“挂”在链表的最后面，扩容、定位、链表环问题）
   2. Hashtable:concurrenthashmap与hashtable
   3. Hashmap什么时候使用红黑树（size>=64&&链表长度达到8），链表环问题，定位逻辑；
   4. Concurrenthashmap中segment大小、读写；get和put需要加锁么，为啥；
2. Set:
   1. HashSet:
        1. 如何set
3. List:
    1. ArrayList:
       1. ArrayList 和 LinkedList的优缺点,ArrayList 和 LinkedList它们谁是线程安全的？
    2. Vector:怎么实现的线程安全

并发：
1. JUC
   1. 原子类实现
2. 进程线程
3. volatile作用和原理，它线程安全吗,volatile如何实现指令重排序,volatile使用场景（读写并发、或可能产生指令重排序问题的地方）；
4. synchronized 和 ReentrantLock的区别：谈谈synchronized 和 ReentrantLock的可重入性
5. Threadlocal内存泄漏问题
6. Atomic类怎么实现的
7. AQS怎么实现的共享锁 ,Synchronized和AQS异同，AQS公平非公平如何实现；
8. 线程池的种类和线程池的组成部分,参数，内部的线程数量变化过程，拒绝策略；
9. 核心工作线程和最大工作线程，超过最大工作线程一定丢弃吗？回来想想应该是考无界队列
10. wait、sleep
11. 为什么要有interruptedException；
12. 线程池中的阻塞队列如果满了怎么办（拒绝策略）
13. 线程的几种状态；
14. 线程池工厂类里面的四个线程池；
15. 多线程里面对一个整型做加减为啥不能用volatile

概念：

1. static代码块执行顺序（父子类），内存泄露问题
2. 抽象类，接口区别
3. equal 和 ==, equal 和 hashcode的关系:如果重写equal没有重写hashcode会发生什么，hashcode一样怎么办？hashcode和equals的区别？如果重写equal没有重写hashcode会发生什么（通过equals方法判断真正的内容。 hashcode和equals的关系如下：1、如果两个对象相等（equals），那么他们一定有相同的哈希值（hash code）。2、如果两个对象的哈希值相等，那么这两个对象有可能相等也有可能不相等。（需要再通过equals来判断），equals:是用来比较两个对象内部的内容是否相等的。==：是用来判断两个对象的地址是否相同，即是否是指相同一个对象。如果没有重写equals时，是直接用==判断的如果是基本类型和基本型封装，则仍然为比较内容。）
1. StringBuilder和StringBuffer区别以及线程安全
2. final和finally，finalize的区别，final能不能保证线程安全
3. try和finally中同时return会返回什么
4. ArrayList 和 LinkedList的优缺点？ArrayList 和 LinkedList它们谁是线程安全的？Arraylist扩容过程，如果频繁扩容会怎么样；
5. Vector怎么实现的线程安全
6. 对面向对象的理解，多态？
7. 值传递 引用传递
8. java字符编码（unicode）；
9. char占多大，能放中文字符么
10. 深浅拷贝、软弱引用

Spring：
1. thymeleaf干什么的
2. spring和springmvc区别
3. springmvc怎么动静分离
4. Spring boot启动过程；
5. dispatcherservlet；
6. 拦截器和过滤器的区别；
7. AOP及实现，为什么需要cglib
8. SSM 是如何整合到项目中的（配置文件（*.xml），用来配置数据源、SpringMVC）
9.  Spring 的启动入口，容器初始化
10. SpringMVC 的 Controller 中返回值有哪些（ModelAndView），如果是异步的请求呢？
11. Spring 的 AOP 
12. Servlet 和 JSP 的区别
13. Servlet 中的 filter 
14. Bean的生命周期；
15. AOP的实现；
16. 什么是DI、为什么DI、DI的类型（构造器注入、方法注入）；
17. Spring boot和spring的差别，tomcat如何嵌入spring boot的/spring boot中的tomcat是如何启动的；
18. Spring如何解决循环依赖问题；

虚拟机：

1. 垃圾回收机制：GC检测可回收对象（判断方法），垃圾收集算法
   1. 怎么判断是垃圾？GCroot可以为哪些？可达性分析
   2. 垃圾回收器了解过吗？介绍一下G1？（所有的回收算法都会存在STW问题，G1的话就是对这个问题进行优化，并行回收，用户很难感知。）
   3. cms和g1的区别，g1结构
2. JVM的内存模型：
   1. 新生代和老年代可以转换（一般来说new一个变量的话都是进入新生代的eden区，但是会有动态对象年龄判定 和长期存活的对象就会进入老年代）
   2. 新生代为什么要设置两个幸存区
   3. 哪些是线程私有的，哪些会被gc影响到
3. 类加载过程
4. 创建一个对象的的过程；
5. 注解的解析在什么时候，生效的时间；
6. Java对象存在哪里？
7. 方法区和直接内存什么时候会oom
8. Jvm调优
9. 如何查看java内存使用情况（jconsole、命令jmap、jstack等等）


数据库：

1. Redis用来做什么
2. MySQL范式，ER图，第四范式
3. MySQL优化，什么是索引，如何创建索引
4. 存储过程，触发器使用过吗，区别
5. 为什么数据库索引用b+树不用b树（底层的话是用b+树实现的，它的优点是能够定位到数据点和范围查询。修改key与子树的组织逻辑，将索引访问都落到叶子节点并 按顺序将叶子节点串起来（方便范围查询））
6. 数据库查询什么时候用不到索引 (要答全面，面试官一直问还有呢？数据库如果在数据很少时，全盘扫描优于索引，也会放弃索引)
7. 数据库基础，索引啊，acid啊，优化啊
8. 多数据源操作怎么保证事务性
9. 讲讲mybatis用了哪些设计模式
10. mysql怎么分页(limit)
11. 事务的特性，以及四种隔离级别
12. Mysql是那种隔离级别，如何防止幻读？（提了一下mvcc，深入下去没讲）
13. mysql数据库的优化 （说到不要使用in 和not in，因为会走全表扫描，接着就问了还有什么和in和not in类似的）
14. 聚簇索引
15. SELECT COUNT(*) 和 SELECT COUNT(某个字段) 有什么区别
16. redis有了解吗？zset 的底层实现
17. 跳表的增删查时间复杂度
18. avl树，红黑树，b+树特征，你觉得为什么redis使用跳表而c++里面的map用的是红黑树（跳表的高度是随机的不稳定）
19. ACID，具体是啥意思
20. 幻读和不可重复读的区别；
21. Mysql和mongodb有啥区别；
22. Mysql的聚合函数
23. Join（inner、left、right）；
24. Union和union all区别；
25. 如何查看语句执行细节（explain）
26. 
建立索引有什么原则
为什么是最左前缀原则
给你ABC三个字段，考虑如何建立索引
mongodb的特点，存储结构
Mysql事务隔离级别，innoDB中repeatable read特点（gap lock）
一个含有insert的事务回滚，里面的自增主键会不会回滚，为什么（不清楚。猜猜？不会。为啥？如果回滚时出现并发问题再插入记录时id可能会变小，不符合增长特性。猜的还挺对）

设计模式：

1. 单例模式：
    1. 多线程情况下如何保证只有一个对象，缺点

数据结构和算法：

1. huffman树的实现 编码
2. AVL树和红黑树的区别
3. b树和b+树的区别
4. 讲讲滑动窗口
5. 冒泡
6. 怎么实现四则运算(带括号)
7. 100万人口的年龄排序
8. 100g文件 3g内存 排序
9. 同时插入1000条数据
10. 给定一个排序链表，删除所有重复的元素，使得每个元素只出现一次。
11. 给定一个二叉树，原地将它展开为链表。
12. 给定一棵二叉树，想象自己站在它的右侧，按照从顶部到底部的顺序，返回从右侧所能看到的节点值。
13. 合并两个有序链表。递归和非递归的实现。
14. 快排
15. 堆排
16. 已知出栈序列求所有的入栈序列
17. 排序算法的稳定性
18. 为什么比较排序只能做到nlg(n)的复杂度
19. 求开方；
20. 循环求斐波那契，时间复杂度；
21. 爬楼梯问题；
22. 抢红包问题，如何保证每个人所得期望一致；
23. 找矩阵中一个2*2的子矩阵，它的元素和是所有相同大小子矩阵中最大的；
24. 顺时针打印2维数组；
25. 判断一个数是不是2的整数次幂；
26. 高效求2的32次幂；
27. 求n!结果中尾部0的个数；
28. 两个不含相同数字的数组，求排序后第k个数，要求复杂度小于O(m+n)（leetcode no.4）；
29. LRU实现（最佳数据结构是链表）；
30. 2叉查找树找两个节点的公共父节点；
31. 实现hashmap的delete（Node）；
32. 二叉树宽度遍历

网路：

1. 三次握手 四次挥手
2. 为什么两倍time_wait
3. TCP和UDP区别
4. ping命令用的什么协议 ICMP
5. bio/nio/aio
6. RPC和http的区别；
7. 输入一个url到返回页面的过程（可以多说一些，比如https、icmp、arp等等）；
8. 幂等性；

linux
1. shell脚本
2. unix如何创建子进程？
3. 什么是僵尸进程，如何解决

git指令
1. commit后要撤销这个操作你怎么做(reset)

项目：
1. 结构设计
2. 

大数据
1. 分布式的缺点
2. Dubbo的一些扩展了解吗
3. ES原理

操作系统
1. 计算机冷启动，内存和寄存器怎么加载的，操作系统看到什么书

中兴