# 概念：
1. 类与多态
   1. 类与抽象类
   2. 抽象类与接口区别
   3. 多态机制
2. equal, ==, hashcode的关系
   1. ==与equal
   2. hashcode和equals的区别
   3. 如果重写equal没有重写hashcode会发生什么?
3. 字符
    1. Strin、StringBuilder和StringBuffer区别
    2. String不是基础类型的原因
    3. String继承
    4. char占多大，能放中文字符么
    5. java字符编码
    6. 常量池
4. 异常
   1. try和finally中同时return会返回什么
   2. final和finally，finalize的区别，final能不能保证线程安全
   3. final特点
   4. Error和Exception区别
5. 数据类型
   1. java八大基本类型，拆箱装箱
   2. 值传递 引用传递
   3. 深浅拷贝
   4. 强引用、弱引用、软引用、虚引用
6. 静态变量
   1. static代码块执行顺序（父子类），内存泄露问题
   2. 覆盖
   3. 静态环境访问非静态变量
7. 反射
   1. 用途
   2. 动态代理

# 集合：
1. List:
    1. ArrayList:
        1. ArrayList和LinkedList的优缺点
        2. ArrayList线程安全
        3. Arraylist扩容过程
        4. ArrayList和Vector
    2. Vector
       1. 怎么实现的线程安全
       2. 三者存储性能
2. Map:
   1. HashMap：
        1. hashmap底层是怎么实现的
        2. Hashmap什么时候使用红黑树，链表环问题，定位逻辑；
        3. Hashmap和Hashtable区别
        4. 线程安全的HashMap
        5. HashMap查找效率
   2. Hashtable:
        1. concurrenthashmap与hashtable
        2. Concurrenthashmap中segment大小、读写；get和put需要加锁么，为啥；
   3. 大量哈希冲突该怎么处理
3. Set:
   1. HashSet:
        1. 如何set
4. collection
   1. collection和map
   2. 集合框架
   3. collection和collections

# 虚拟机：

1. 内存模型
   1. 结构
   2. 新生代为什么要设置两个幸存区
   3. 新生代和老年代转换
   4. JVM的局部变量表是做什么的
   5. Java对象存在哪里
   6. 字节码结构
2. 垃圾回收
   1. 概念
   2. 垃圾判断方法
   3. GCroot为哪些
   4. 垃圾收集算法
   5. 垃圾回收器
   6. cms和g1的区别，g1结构
   7. 主动回收
3. 类加载
   1. 类的生命周期
   2. 类加载器
   3. 双亲委派模型，优点
   4. 创建对象的过程，自定义加载器
   5. 类加载器的类的缓存key是什么
   6. 加载class原理
4. 什么是虚拟机
5. 堆与栈的区别
6. 方法区和直接内存oom
7. Jvm调优
8. 查看java内存使用情况                

# 并发：
1. 进程线程
   1. 概念
   2. 线程的几种状态
   3. 创建方式，callable返回值是什么
   4. run和start方法区别
   5. wait、sleep
   6. 线程同步的方法
2. volatile
   1. volatile作用和原理，它线程安全吗,volatile如何实现指令重排序,volatile使用场景
   2. volatile 可以保证并发计数正确性？不能
   3. 多线程里面对一个整型做加减为啥不能用volatile
3. Synchronized
   1. synchronize的底层实现原理，用法，
   2. 修饰类方法和普通方法的锁区别,获取类锁之后还能获取对象锁吗
   3. synchronized 和 ReentrantLock的区别：谈谈synchronized 和 ReentrantLock的可重入性
4. 线程池
   1. 为什么要线程池？
   2. 线程池的中的线程数量能不能无限增加，为什么?不能，线程需要一定开销，太多线程会耗尽计算机资源，（而且过多线程也无法发挥多线程的优势，毕竟cpu核就那么多）
   3. java 里任务提交给线程池后，那些任务是存储在哪的?
   4. 阻塞队列怎么实现？
   5. 线程池ThreadPoolExecutor
   6. 线程池中的阻塞队列如果满了怎么办
   7. 线程池工厂类里面的四个线程池
   8. 线程池及其任务调度算法
   9. 线程池的种类和线程池的组成部分,参数，内部的线程数量变化过程，拒绝策略；
   10. 核心工作线程和最大工作线程，超过最大工作线程一定丢弃吗？
5. JUC
   1. AQS怎么实现的共享锁 ,Synchronized和AQS异同，AQS公平非公平如何实现；
   2. 如果需要保证并发计数正确怎么办，只能加锁吗？ AtomicInteger。
   3. Atomic类怎么实现的
6. 线程安全
   1. 原子类实现
7.  Threadlocal内存泄漏问题
8.  为什么要有interruptedException；
9.  进程同步模型，以及并发控制代码（手撕），注意主要写法在于wait的时候需要在while条件内进行，而notifyall后则会改变此条件，从而达到锁配对
10. 自旋和自适应自旋
11. 为什么产生死锁
12. bio/nio/aio

# 设计模式：

1. 单例模式：
    1. 如何确保单例线程安全，多线程情况下如何保证只有一个对象，缺点
2. 状态机模式
    1. 如何解决状态机不对称
3. 策略模式的使用场景

# 网路：

1. 三次握手 四次挥手
2. 为什么两倍time_wait
3. TCP和UDP区别
4. ping命令用的什么协议 ICMP是TCP/IP协议簇的一个子协议
5. RPC和http的区别,HTTPS；
6. 输入一个url到返回页面的过程（可以多说一些，比如https、icmp、arp等等）；
7. 幂等性；
8. 如何保证TCP连接中发包有序且不丢失（超时检测，序号乱序，数据错误，重复发包）
9. 进程间如何通信，信号和通道的区别
10. session和cookies区别，session如何管理

# 数据库：

1. Redis
   1. 用途
   2. 如何将数据分布在不同的Redis
   3. zset的底层实现
2. 原理
   1. 范式
   2. ER图
3. MySQL
   1. 怎么分页(limit)
   2. 聚合函数
   3. SELECT COUNT(*) 和 SELECT COUNT(某个字段) 有什么区别
   4. Join
   5. Union和union all区别；
   6. 如何查看语句执行细节
   7. mysql 的读写分离,读写分离有什么用？
   8. 存储过程，触发器使用过吗，区别
4. 数据结构
   1. MySQL优化
      1. MySQL查询优化
   2. 事物隔离
      1. ACID
      2. Mysql事务隔离级别
      3. 如何防止幻读，幻读和不可重复读的区别；
      4. 一个含有insert的事务回滚，里面的自增主键会不会回滚，为什么
      5. innoDB中repeatable read特点（gap lock）*
   3. 索引
      1. 什么是索引，如何创建索引
      2. 聚簇索引，数据库查询什么时候用不到索引，多个索引会有多份数据吗
      3. 建立索引有什么原则；给你ABC三个字段，考虑如何建立索引；最左前缀原则
      4. B树，B+树，为什么数据库索引用b+树不用b树
      5. MySQL使用的索引结构，查找效率
      6. 底层数据存储原理
   4. 跳表的复杂度。avl树，红黑树，b+树特征，你觉得为什么redis使用跳表而c++里面的map用的是红黑树（跳表的高度是随机的不稳定）*
   5.  B树怎么保证每次操作完都平衡的？*
5. Mybatis
   1. 用了哪些设计模式*
6. 锁
   1. 乐观锁*
   2. 如果有多个变量要更新，要保证一致性，怎样加锁来保证正确性，效率又比较高？只在写时加锁，读不加锁……*
   3. 那怎么解决一个写操作修改了部分变量，读操作，读取了这个中间状态的问题？emmm写操作时，先锁住锁1，在线程本地也就是函数局部计算完所有结果后，锁住锁2，一次更新完后再释放2个锁，读操作只锁住锁2……*

# Spring：
1. 概念
   1. thymeleaf干什么的
   2. AOP及实现，为什么需要cglib
   3. 拦截器和过滤器的区别
   4. Spring 的启动入口，容器初始化*
   5. Servlet 和 JSP 的区别
   6. Servlet 3.0 新特性？只知道一个注解（注解、异步、还有个插件还是什么的）*
   7. Servlet 的生命周期
   8. 如何解决循环依赖问题；
   9. 什么是DI、为什么DI，DI的类型（）；
   10. Bean的生命周期；
   11. bean的默认范围
2. SpringMVC
   1. spring和springmvc区别
   2. springmvc动静分离
   3. MVC过程，controller干什么的
   4. SpringMVC 的 Controller 中返回值有哪些（ModelAndView），如果是异步的请求呢？*
   5. SpringMVC 处理请求的整个流程？入口？*
   6. 浏览器输入网址过程,结合springmvc*
   7. dispatcherservlet*
3. SpringSSM
   1. 介绍
   2. SSM 是如何整合到项目中的*
4. SpringBoot
   1. 启动过程；
   2. Spring boot和spring的差别，tomcat如何嵌入spring boot的/spring boot中的tomcat是如何启动的；
5. 对Netty的了解*

# 数据结构和算法：

1. 排序
   1. 冒泡
   2. 快排，时间复杂度，空间复杂度
   3. 堆排
   4. 排序算法的稳定性
   5. 为什么比较排序只能做到nlg(n)的复杂度
2. 树
   1. 给定一个二叉树，原地将它展开为链表。
   2. 给定一棵二叉树，想象自己站在它的右侧，按照从顶部到底部的顺序，返回从右侧所能看到的节点值。
   3. 二叉树宽度遍历
   4. 二叉查找树如何变成平衡树
   5. 一个二叉树，每个节点除了有左右子节点外，还有指向父节点的引用。给出一个节点，返回它在二叉树中中序遍历的下一个节点。
   6. 2叉查找树找两个节点的公共父节点；
   7. huffman树的实现 编码
   8. AVL树和红黑树的区别
   9. b树和b+树的区别
   10. 红黑树比BST优点
3. 链表
   1. 给定一个排序链表，删除所有重复的元素，使得每个元素只出现一次。
   2. 合并两个有序链表。递归和非递归的实现。
   3. 数组和链表的区别
   4. LRU实现（最佳数据结构是链表）；
4. 数据
   1. 100万人口的年龄排序
   2. 100g文件 3g内存 排序
   3. 同时插入1000条数据
5. 数组、栈
   1. 已知出栈序列求所有的入栈序列
   2. 顺时针打印2维数组；
   3. 两个不含相同数字的数组，求排序后第k个数，要求复杂度小于O(m+n)（leetcode no.4）；
   4. 
6. 讲讲滑动窗口
7. 怎么实现四则运算(带括号)
8. 求开方；
9. 循环求斐波那契，时间复杂度；
10. 爬楼梯问题；
11. 抢红包问题，如何保证每个人所得期望一致；
12. 找矩阵中一个2*2的子矩阵，它的元素和是所有相同大小子矩阵中最大的；
13. 判断一个数是不是2的整数次幂；
14. 高效求2的32次幂；
15. 求n!结果中尾部0的个数；
16. 实现hashmap的delete（Node）；
17. 实现一个 hashtable。我首先说了下思路，然后先写了个简单的，碰撞冲突采用的拉链法，注意一下边界条件的处理，null什么的。如果元素变多会怎样？碰撞冲突会增多，链表会边长，效率变低。怎么解决？达到一定数量的时候，对数组扩容，然后把所有元素重新hash一遍。保证线程安全怎么做？最简单的直接方法上加synchronized关键字。
18. 广度优先和深度优先，怎么用代码实现(广度优先是层级遍历，采用具有先进先出特点的队列进行辅助实现,深度优先是先遍历完子树，采用具有先进后出特点的栈进行辅助实现)

linux
1. shell脚本
2. unix如何创建子进程？
3. 什么是僵尸进程，如何解决

git指令
1. commit后要撤销这个操作你怎么做(reset)

项目：
1. 结构设计
2. handle消息分发机制

大数据
1. 分布式的缺点
2. Dubbo的一些扩展了解吗
3. ES原理
4. Mysql和mongodb区别；mongodb的特点，存储结构

操作系统
1. 计算机冷启动，内存和寄存器怎么加载的，操作系统看到什么书

十面阿里腾讯部分